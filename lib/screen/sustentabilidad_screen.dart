import 'package:flutter/material.dart';

class SustentabilidadScreen extends StatelessWidget {
  const SustentabilidadScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SustentabilidadScreen'),
      ),
      body: const Center(
        child: Text('SustentabilidadScreen'),
      ),
    );
  }
}
